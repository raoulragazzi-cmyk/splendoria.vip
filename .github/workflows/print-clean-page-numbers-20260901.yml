name: Splendoria clean book print

on:
  push:
    branches: ["splendoria.vip"]
    paths: [".github/workflows/print-clean-page-numbers-20260901.yml"]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: splendoria.vip
          fetch-depth: 0
      - run: python .github/scripts/print_clean_page_numbers_20260901.py
      - run: npm ci
      - run: |
          node --check src/worker.js
          npm run check
      - name: Commit application source
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/worker.js
          git diff --cached --check
          git commit -m "fix: clean book print headers and add page numbers"
          git push origin HEAD:splendoria.vip
