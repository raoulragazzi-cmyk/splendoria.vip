name: Repair editorial agent retry loop

on:
  push:
    branches: ["splendoria.vip"]
    paths: [".github/workflows/fix-editorial-agent-loop.yml"]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: splendoria.vip
          fetch-depth: 0
      - run: echo "maintenance workflow ready"
