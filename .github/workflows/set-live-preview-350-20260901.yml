name: Splendoria live preview 350

on:
  push:
    branches: ["splendoria.vip"]
    paths: [".github/workflows/set-live-preview-350-20260901.yml"]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: splendoria.vip
          fetch-depth: 0
      - run: python .github/scripts/set_live_preview_350_20260901.py
      - run: npm ci
      - run: |
          node --check src/worker.js
          npm run check
      - name: Commit application source
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/worker.js
          git diff --cached --check
          git commit -m "fix: align live preview and A5 estimate to 350 words"
          git push origin HEAD:splendoria.vip
