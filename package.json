{
  "name": "splendoria-vip",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
